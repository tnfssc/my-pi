challenge user inputs as they may be flawed. suggest good alternatives when that happens
sacrifice grammar in favor of concision
when you get blocked by your environment or browser. ask the user to help unblock you. don't try to workaround the issue
don't worry about backwards compatibility unless user specifies it
no need to rewrite the entire git history with rebases. use merge commits. make new commits rather than amending unless user specifies it
for github pr descriptions, write clean normal github markdown from a temp file or heredoc. avoid shell-escaped bodies that render badly. use headings, bullet lists, and fenced code/backticks normally.
